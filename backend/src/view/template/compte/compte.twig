<!DOCTYPE html>
<html lang="fr">
<head>
  <title>Geoquizz</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="{{style}}/css/input.css">
  <link rel="stylesheet" href="{{style}}/css/style.css">
  <link rel="stylesheet" href="{{style}}/css/background.css">
</head>
<body>
  <header class="navbar">
    <div class="nav_element">
        <a href="{{backoffice}}"><img src="{{style}}/img/logo_chico.png" class="logo"><h6>Geoquizz</h6></a>
    </div>
    <nav class="nav_element">
      <a href="{{logout}}">Déconnexion <span class="icon-exit"></span></a>
      <a href="{{compte}}">Compte <span class="icon-cog"></span></a>
    </nav>
  </header>
  <article class="container">
    <div class="jbt col-6">
      {% if exception != null %}
        {% if exception is iterable %}
          {% for excep in exception %}
            <p>{{excep.0}}</p>
          {% endfor %}
        {% else %}
          <p>{{exception}}</p>
        {% endif %}
      {% endif %}
      <h1>Modification du compte</h1>
      <form class="form-horizontal" method="POST" action="{{modifier}}">
        <div class="form-group">
            <input type="text" class="form-control" id="nom" placeholder="Nom" name="nom" value="{{compteGet.0.nom}}" required>
        </div>
        <div class="form-group">
            <input type="email" class="form-control" id="email" placeholder="Email" name="email" value="{{compteGet.0.email}}" required>
        </div>
        <div class="form-group">
            <input type="password" class="form-control" id="password_old" placeholder="Ancien mot de passe" name="password_old" required>
        </div>
        <div class="form-group">
            <input type="password" class="form-control" id="password" placeholder="Mot de passe" name="password" required>
        </div>
        <div class="form-group">
            <input type="password" class="form-control" id="password_rep" placeholder="Mot de passe passe" name="password_rep" required>
        </div>
        <div class="form-group">
            <button type="submit" class="btn btn-default">Modifier !</button>
        </div>
     </form>
    </div>
   </article>
</body>
</html>
