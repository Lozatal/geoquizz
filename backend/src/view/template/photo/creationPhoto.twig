<!DOCTYPE html>
<html lang="fr">
<head>
  <title>Geoquizz</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="{{style}}/css/input.css">
  <link rel="stylesheet" href="{{style}}/css/style.css">
</head>
<body>
  <header class="navbar">
    <div class="nav_element">
        <a href="{{backoffice}}"><img src="{{style}}/img/logo_chico.png" class="logo"><h6>Geoquizz</h6></a>
    </div>
    <nav class="nav_element">
      <a href="{{logout}}">Déconnexion <span class="icon-exit"></span></a>
      <a href="{{compte}}">Compte <span class="icon-cog"></span></a>
    </nav>
  </header>

  <article class="container">
    <div class="jbt">
      {% if exception != null %}
        {% if exception is iterable %}
          {% for excep in exception %}
            <p>{{excep.0}}</p>
          {% endfor %}
        {% else %}
          <p>{{exception}}</p>
        {% endif %}
      {% endif %}
      <h1>Ajouter une nouvelle photo à la série</h1>
      <div class="flex">
        <form class="form-horizontal" method="POST" action="{{creation}}">
         <div class="form-group">
             <input type="text" class="form-control" id="description" placeholder="Nom et/ou détails sur l'image" name="description" required>
         </div>
         <div class="form-group">
             <input type="text" class="form-control" id="position_long" placeholder="Longitude" name="position_long" required>
         </div>
         <div class="form-group">
             <input type="text" class="form-control" id="position_lat" placeholder="Latitude" name="position_lat" required>
         </div>
         <div class="form-group">
             <p><a href="https://imageshack.us/" target="_blank">Upload de l'image</a></p>
             <input type="text" class="form-control" id="url" placeholder="Lien vers l'image" name="url" required>
         </div>
         <div class="form-group">
             <input type="hidden" class="form-control" id="id_serie" name="id_serie" value="{{idSerie}}">
             <input type="hidden" class="form-control" id="lat_serie" name="lat_serie" value="{{latSerie}}">
             <input type="hidden" class="form-control" id="long_serie" name="long_serie" value="{{longSerie}}">
             <button type="submit" class="btn btn-default">Ajouter !</button>
         </div>
       </form>
       <div id="map"></div>
      </div>
    </div>
</article>
<script src="{{style}}/js/map.js" charset="utf-8"></script>
<script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyDW6k5H-IUwCs5HrPIUWz0NWfC41fQWz2Y&callback=initMap"async defer></script>
</body>
</html>
