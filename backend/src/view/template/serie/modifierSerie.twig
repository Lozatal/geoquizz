<!DOCTYPE html>
<html lang="fr">
<head>
  <title>Geoquizz</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="{{style}}/css/input.css">
</head>

<body>
  <header class="navbar">
    <div class="nav_element">
        <a href="{{backoffice}}"><img src="{{style}}/img/logo_chico.png" class="logo"></a>
    </div>
    <nav class="fondo">
      <nav class="nav_element">
        <a href="#news">Déconnexion</a>
      </nav>
    </nav>
  </header>

  <article class="container">
    <div class="jumbotron">
      <h1>Modifier une série</h1>
      <form class="form-horizontal" method="POST" action="{{modification}}">
       <div class="form-group">
         <label class="control-label col-sm-2" for="ville">Ville:</label>
         <div class="col-sm-10">
           <input type="text" class="form-control" id="ville" placeholder="ville" name="ville" value="{{serie.ville}}">
         </div>
       </div>
       <div class="form-group">
         <label class="control-label col-sm-2" for="map_refs">Coordonnées de la ville:</label>
         <div class="col-sm-10">
           <input type="text" class="form-control" id="map_refs" placeholder="Localisation GPS de la ville" name="map_refs" value="{{serie.map_refs}}">
         </div>
       </div>
       <div class="form-group">
         <label class="control-label col-sm-2" for="dist">Distance de marquage de points:</label>
         <div class="col-sm-10">
           <input type="text" class="form-control" id="dist" placeholder="Distance en mèter" name="dist" value="{{serie.dist}}">
         </div>
       </div>
       <div class="form-group">
         <div class="col-sm-offset-2 col-sm-10 dividir">
           <button type="submit" class="btn btn-default">Modifier !</button>
         </div>
       </div>
     </form>
    </div>
   </article>
</body>
</html>
